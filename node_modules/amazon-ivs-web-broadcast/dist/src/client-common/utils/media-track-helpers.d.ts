/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { Maybe } from '../types';
/**
 * Determines what kind of video track we are dealing with.
 *
 * @param videoTrackSettings - The settings from a video track.
 * @private
 */
export declare const getVideoTrackType: (videoTrackSettings: MediaTrackSettings) => "unknown" | "tab" | "window" | "screen" | "camera";
/**
 * Looks through the array of provided tracks for MediaStreamTrack matching the provided kind.
 *
 * @param tracks - Array of MediaStreamTracks.
 * @param kind - Determines what kind of track we want of type [[TrackKinds]].
 * @private
 */
export declare function getTrackOfType(tracks: MediaStreamTrack[], type: TrackType): Maybe<MediaStreamTrack>;
export declare enum TrackType {
    VIDEO = "video",
    AUDIO = "audio"
}
