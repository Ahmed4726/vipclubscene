/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { LogLevels } from './logger/logger.types';
/** @hidden */
export declare const globalOverrideKey = "IVS_CLIENT_OVERRIDES";
/** @hidden */
export interface OverrideConfig {
    logLevel?: LogLevels;
    appLabel?: string;
    whipUrl?: string;
    disableXdp?: boolean;
    analyticsPostUrl?: string;
    node?: string;
    dataPlaneEndpoint?: string;
}
/**
 * Internal API to read if logLevel override config has been specified.
 *
 * @hidden
 * @returns - LogLevels of the configured logLevel if specified, else
 * undefined.
 */
export declare function getOverrideLogLevel(): LogLevels | undefined;
/**
 * Internal API to read if app label override config has been specified.
 *
 * @hidden
 * @returns - String of the configured app label if specified, else
 * undefined.
 */
export declare function getOverrideAppLabel(): string | undefined;
/**
 * Internal API to read if an override of the whip url has been specified in the
 * override config.
 *
 * @hidden
 * @returns - String of the configured whip url if specified, else undefined.
 */
export declare function getOverrideWhipUrl(): string | undefined;
/**
 * Internal API to read if XDP feature has been disabled by client override
 * config.
 *
 * @hidden
 * @returns - Boolean if specified in override config, else undefined.
 */
export declare function isXdpDisabled(): boolean | undefined;
/**
 * Internal API to read if they analytics post url override config has been specified.
 *
 * @hidden
 * @returns - String of the configured app label if specified, else
 * undefined.
 */
export declare function getOverrideAnalyticsPostUrl(): string | undefined;
/**
 * Internal API to read if a Mercury node override has been specified (i.e. and
 * utilize Mercury's ability to "force" a node in staging).
 *
 * @hidden
 * @returns - String of the node if specified, else undefined.
 */
export declare function getOverrideGlobalForceNode(): string | undefined;
