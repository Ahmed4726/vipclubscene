/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { SubscribeType } from '../../../stage/core-api/stage-subscription';
import { StageParticipantInfo } from '../stage-participant/stage-participant';
import { LocalStageStream } from '../stage-stream';
export declare const defaultStrategy: {
    stageStreamsToPublish(): never[];
    shouldPublishParticipant(participant: StageParticipantInfo): boolean;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): SubscribeType;
};
export declare const nonPublishingStrategy: {
    stageStreamsToPublish(): never[];
    shouldPublishParticipant(participant: StageParticipantInfo): boolean;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): SubscribeType;
};
export declare const publishingStrategy: {
    stageStreamsToPublish(): LocalStageStream[];
    shouldPublishParticipant(participant: StageParticipantInfo): boolean;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): SubscribeType;
};
export declare const publishingStrategyWithStageStreamsError: {
    stageStreamsToPublish(): never;
    shouldPublishParticipant(participant: StageParticipantInfo): boolean;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): SubscribeType;
};
export declare const publishingStrategyWithShouldPublishError: {
    stageStreamsToPublish(): LocalStageStream[];
    shouldPublishParticipant(participant: StageParticipantInfo): never;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): SubscribeType;
};
export declare const subscribeOnlyStrategyWithError: {
    stageStreamsToPublish(): never[];
    shouldPublishParticipant(participant: StageParticipantInfo): boolean;
    shouldSubscribeToParticipant(participant: StageParticipantInfo): never;
};
