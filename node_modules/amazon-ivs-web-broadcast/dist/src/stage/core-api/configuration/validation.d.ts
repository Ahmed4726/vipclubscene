/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { DeepPartial } from '../utils/types';
import { StageAudioConfiguration, StageStreamConfiguration, StageVideoConfiguration, SubscribeConfiguration } from './schema';
/**
 * Normalizes an incoming customer defined audio or video stream
 * configuration against defaults, and errors if values are not
 * within expected bounds.
 *
 * @param track - Audio or video track being configured
 * @param config - Optional customer defined config object
 */
export declare function normalizeStreamConfig(track: MediaStreamTrack, config?: StageVideoConfiguration | StageAudioConfiguration): StageStreamConfiguration;
/**
 * Normalize a subscribe config instance, clamping values where needed
 * The provided config can be complete or partial
 *
 * Any needed changes are done in a copy of the given config.
 *
 * @param config - The subscribe config to normalize
 * @return The normalized subscribe config
 */
export declare const normalizeSubscribeConfig: (config: DeepPartial<SubscribeConfiguration>) => DeepPartial<SubscribeConfiguration>;
