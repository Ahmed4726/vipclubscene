/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
export interface SimulcastConfiguration {
    /**
     * Whether to enable or disable Simulcast as a publisher
     *
     * @default false
     */
    enabled: boolean;
}
export interface StageVideoConfiguration {
    /**
     * Minimum bitrate in Kbps. NOTE: At this time this
     * property is a no-op and is marked as a known gap.
     */
    minBitrate?: number;
    /**
     * Maximum bitrate in Kbps
     *
     * Default: [[STAGE_MAX_BITRATE]]
     * Max: [[STAGE_MAX_BITRATE]]
     */
    maxBitrate?: number;
    /**
     * Frames per second
     *
     * Default: [[STAGE_MAX_FRAMERATE]]
     * Max: [[STAGE_MAX_FRAMERATE]]
     */
    maxFramerate?: number;
    /**
     * Configuration for Simulcast (Layered Encoding)
     */
    simulcast?: SimulcastConfiguration;
}
export interface StageAudioConfiguration {
    /**
     * Max bitrate of the audio stream in Kbps. Value is limited as follows:
     *
     * Min: [[STAGE_MIN_AUDIO_BITRATE_KBPS]]
     * Max: [[STAGE_MAX_AUDIO_BITRATE_KBPS]]
     * Default: [[STAGE_MAX_AUDIO_BITRATE_KBPS_DEFAULT]]
     */
    maxAudioBitrateKbps?: number;
    /**
     * Explicitly enable stereo (true) or mono (false) for the audio stream.
     *
     * Note: that if using a audio source retrieved with getUserMedia you must
     * disable autoGainControl, echoCancellation, and noiseSuppression
     * constraints when enabling stereo.
     *
     * @default false
     */
    stereo?: boolean;
}
/**
 * Consolidated stream type for audio or video tracks
 */
export declare type StageStreamConfiguration = Required<StageVideoConfiguration> & Required<StageAudioConfiguration>;
/****************************
 * Subscribe Configuration  *
 ****************************/
/**
 * @internal
 * Jitter buffer min delay presets
 */
export declare enum JitterBufferMinDelay {
    DEFAULT = "default",
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high"
}
/**
 * @internal
 * Jitter buffer configuration, internal model
 */
export interface JitterBufferConfigration {
    minDelayWhenPublishing: JitterBufferMinDelay | number;
    minDelayWhenSubscribeOnly: JitterBufferMinDelay | number;
}
/**
 * @internal
 * Subscribe configuration, internal model
 */
export interface SubscribeConfiguration {
    jitterBuffer: JitterBufferConfigration;
}
/****************************
 * Composite types          *
 ****************************/
/**
 * @internal
 */
export declare type AllConfiguration = StageStreamConfiguration | SubscribeConfiguration;
