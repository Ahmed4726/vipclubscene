import { type StageToken } from '../token';
import { StageAnalyticsEvent } from './event';
import { TraceId } from './random.uuid';
export declare class JoinEvent extends StageAnalyticsEvent {
    constructor(properties: {
        token: StageToken;
        eventEndpoint: string;
        whipEndpoint: string;
        traceId: TraceId;
    });
}
export declare class JoinAttemptEvent extends StageAnalyticsEvent {
    constructor(properties: {
        token: StageToken;
        eventEndpoint: string;
        whipEndpoint: string;
        traceId: TraceId;
    });
}
export declare class JoinFailedEvent extends StageAnalyticsEvent {
    constructor(properties: {
        token: StageToken;
        eventEndpoint: string;
        whipEndpoint: string;
        traceId: TraceId;
        code: number;
        message: string;
        fatal: boolean;
        nominal: boolean;
        retryCnt: number;
    });
}
