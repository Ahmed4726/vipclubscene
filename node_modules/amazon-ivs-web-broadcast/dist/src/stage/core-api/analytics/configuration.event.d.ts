/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { type StageToken } from '../token';
import { StageAnalyticsEvent } from './event';
import { TraceId } from './random.uuid';
export declare type MultihostConfigurationPayload = {
    video_codec: string;
    width: number;
    height: number;
    max_bitrate: number;
    min_bitrate: number;
    target_fps: number;
    echo_cancellation: boolean;
    auto_gain: boolean;
    noise_suppression: boolean;
    enable_simulcast: boolean;
    max_audio_bitrate_bps: number;
    stereo?: boolean;
};
export declare class MultihostConfigurationEvent extends StageAnalyticsEvent {
    constructor(token: StageToken, traceId: TraceId, payload: MultihostConfigurationPayload, action: string);
}
