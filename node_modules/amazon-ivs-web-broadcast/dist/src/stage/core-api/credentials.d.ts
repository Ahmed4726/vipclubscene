import { AnalyticsTracker } from '../../client-common/analytics/analytics-tracker';
import { TraceId } from './analytics/random.uuid';
import { Action } from './constants';
export declare type StageCapabilities = {
    simulcastNegotiated: boolean;
};
declare function getTurnCredentials({ action, identifier, token, traceId, tag, configFeatures, analyticsTracker, }: {
    action: Action;
    identifier: string;
    token: string;
    traceId: TraceId;
    tag: string;
    configFeatures: {
        simulcastEnabled: boolean;
    };
    analyticsTracker: AnalyticsTracker;
}): Promise<{
    iceCredentials: RTCIceServer[];
    responseURL: any;
    stageCapabilities: StageCapabilities;
} | undefined>;
export { getTurnCredentials };
