/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { WHIPServerResponse } from '../../../client-common/types';
import { TraceId } from '../analytics/random.uuid';
import { StageToken } from '../token';
export interface SdpExchanger {
    exchange(options: {
        sdpOffer: string;
        url: string;
        traceId: TraceId;
        forceNode?: string;
    }): Promise<WHIPServerResponse | undefined>;
    isSupported(): boolean;
    getTransport(): string;
}
export declare abstract class BaseSdpExchanger implements SdpExchanger {
    protected token: StageToken;
    constructor(token: StageToken);
    abstract exchange(options: {
        sdpOffer: string;
        url: string;
        traceId: TraceId;
        forceNode?: string;
    }): Promise<WHIPServerResponse | undefined>;
    abstract isSupported(): boolean;
    abstract getTransport(): string;
}
